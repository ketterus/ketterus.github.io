{
  "name": "ketterus-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf _site",
    "build": "npx @11ty/eleventy",
    "start": "npx @11ty/eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}